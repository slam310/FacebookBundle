function fbLogin(callbackFnc, setAccessToken){
  	if (typeof (FB) != 'undefined' && FB != null) {
  		FB.getLoginStatus(function(response) {
  			if (response.status === 'connected')
  			  setAccessToken(response.authResponse.accessToken);
  			else
  			  FB.login();
  		});
  	}
}